import { img, string, array } from 'prop-types';
import React from 'react';

export function Player({nome, equipe, listaDeCartas}) {
    return (                                
            <div></div>                    
    );
}

Player.propTypes = {
    source: img,
     nome: string, 
     equipe: string, 
     listaDeCartas: array
  }



